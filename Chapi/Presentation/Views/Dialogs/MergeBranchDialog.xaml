<UserControl x:Class="Chapi.Presentation.Views.Dialogs.MergeBranchDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Width="500" Height="450">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style x:Key="BranchItemStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDivider}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource MaterialDesignSelection}"/>
                </Trigger>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryHueLightBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Search -->
            <RowDefinition Height="*"/>    <!-- List -->
            <RowDefinition Height="Auto"/> <!-- Footer Status & Actions -->
        </Grid.RowDefinitions>

        <!-- 1. HEADER -->
        <Border Grid.Row="0" Padding="16" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,4" >
                    <TextBlock Text="{Binding MergeType}" FontWeight="Bold" Opacity="0.7" FontSize="16"/>
                    <TextBlock Text=" desde " Opacity="0.7" FontSize="16"/>
                    <TextBlock Text="{Binding CurrentBranch}" FontWeight="SemiBold" FontSize="16"/>
                    <TextBlock Text=" hacia..." Opacity="0.7" FontSize="16"/>
                </StackPanel>
                <TextBlock Text="Selecciona la rama de destino" 
                           FontSize="14" Opacity="0.5" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>

        <!-- 2. SEARCH -->
        <Border Grid.Row="1" Padding="16,12">
            <TextBox materialDesign:HintAssist.Hint="Filtrar ramas"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"                     
                     materialDesign:TextFieldAssist.DecorationVisibility="Collapsed"
                     BorderBrush="{DynamicResource MaterialDesignDivider}"/>
        </Border>

        <!-- 3. LIST -->
        <Border Grid.Row="2" Margin="16,0,16,0" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1" CornerRadius="4">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ListBox ItemsSource="{Binding FilteredBranches}" 
                         SelectedItem="{Binding SelectedBranch}"
                         Background="Transparent"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border Style="{StaticResource BranchItemStyle}">
                                            <ContentPresenter/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Icono Branch -->
                                <materialDesign:PackIcon Kind="SourceBranch" 
                                                       Grid.Column="0" 
                                                       VerticalAlignment="Center" 
                                                       Margin="0,0,12,0"
                                                       Opacity="0.7"/>

                                <!-- Nombre y Detalle -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontWeight="Medium"/>
                                    <TextBlock Text="Rama predeterminada" 
                                               FontSize="10" Opacity="0.5"
                                               Visibility="{Binding IsDefault, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                </StackPanel>

                                <!-- Icono Check si es el seleccionado -->
                                <materialDesign:PackIcon Kind="Check" 
                                                       Grid.Column="2" 
                                                       VerticalAlignment="Center" 
                                                       Visibility="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                       Foreground="{DynamicResource MaterialDesignBody}"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>
        </Border>

        <!-- 4. FOOTER (Status & Actions) -->
        <Border Grid.Row="3" Padding="16" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,1,0,0" Margin="0,16,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Info Description -->
                    <RowDefinition Height="Auto"/> <!-- Status Area -->
                    <RowDefinition Height="Auto"/> <!-- Buttons -->
                </Grid.RowDefinitions>

                <!-- Info Description -->
                <StackPanel Grid.Row="0" Margin="24,0,24,12" Orientation="Vertical">
                    <Grid Opacity="0.6" HorizontalAlignment="Stretch" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <materialDesign:PackIcon Kind="InformationOutline" 
                                               Grid.Column="0"
                                               VerticalAlignment="Center" 
                                               Margin="0,0,8,0"
                                               Width="16" Height="16"/>
                                               
                        <TextBlock Grid.Column="1" 
                                   Text="{Binding MergeDescription}"
                                   FontSize="11"
                                   Foreground="{DynamicResource MaterialDesignBody}"
                                   TextWrapping="Wrap"
                                   VerticalAlignment="Center"
                                   TextAlignment="Left"/>
                    </Grid>

                    <CheckBox IsChecked="{Binding IsDeleteSourceBranchChecked}"
                              Content="Eliminar rama origen después de fusionar (Recomendado)"
                              FontSize="12"
                              Visibility="{Binding IsDeleteOptionVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                              ToolTip="Limpia la rama después de fusionarla para evitar confusiones."/>
                </StackPanel>

                <!-- Status Area -->
                <StackPanel Grid.Row="1" Margin="0,0,0,16" Orientation="Horizontal" HorizontalAlignment="Center">
                    <!-- Loading -->
                    <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                                 Value="0" IsIndeterminate="True"
                                 Width="16" Height="16"
                                 Margin="0,0,8,0"
                                 Visibility="{Binding IsCheckingStatus, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                 
                    <!-- Success Icon -->
                    <materialDesign:PackIcon Kind="CheckCircle" 
                                           Foreground="{DynamicResource StatusSuccessBrush}" 
                                           VerticalAlignment="Center" Margin="0,0,8,0">
                        <materialDesign:PackIcon.Style>
                            <Style TargetType="materialDesign:PackIcon">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding CanMerge}" Value="True"/>
                                            <Condition Binding="{Binding IsCheckingStatus}" Value="False"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </materialDesign:PackIcon.Style>
                    </materialDesign:PackIcon>

                    <!-- Warning/Error Icon -->
                    <materialDesign:PackIcon Kind="AlertCircle" 
                                           Foreground="{DynamicResource StatusWarningBrush}" 
                                           VerticalAlignment="Center" Margin="0,0,8,0">
                        <materialDesign:PackIcon.Style>
                             <Style TargetType="materialDesign:PackIcon">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasConflicts}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </materialDesign:PackIcon.Style>
                    </materialDesign:PackIcon>

                    <!-- Status Text -->
                    <TextBlock Text="{Binding StatusMessage}" 
                               VerticalAlignment="Center" 
                               FontSize="12"
                               Opacity="0.8"
                               TextWrapping="Wrap"
                               MaxWidth="380"/>
                </StackPanel>

                <!-- Buttons -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancelar" 
                            Style="{StaticResource MaterialDesignFlatButton}" 
                            Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                            Margin="0,0,8,0"/>
                            
                    <Button Content="{Binding ActionButtonText}" 
                            Command="{Binding ConfirmCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Width="200"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
