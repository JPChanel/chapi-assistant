<Window x:Class="Chapi.Presentation.Views.Dialogs.ExecutionLogDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d" 
        d:DesignHeight="450" d:DesignWidth="800"
        Width="800" Height="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    
    <!-- Borde principal con fondo para simular ventana -->
    <Border Background="{DynamicResource MaterialDesignPaper}" CornerRadius="8" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1" Margin="10">
        <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <materialDesign:PackIcon Kind="Console" Width="24" Height="24" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{DynamicResource HeaderForeground}"/>
                <TextBlock Text="{Binding Title, FallbackValue='Ejecutando Tarea...'}" Foreground="{DynamicResource HeaderForeground}" Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
        </StackPanel>

        <!-- Progress Bar -->
        <ProgressBar Grid.Row="1" IsIndeterminate="{Binding IsRunning}" Height="4" Margin="0,0,0,8" 
                     Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}"/>

        <!-- Console Output -->
        <Border Grid.Row="2" Background="{DynamicResource MaterialDesignCardBackground}" CornerRadius="4" Padding="4" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1">
            <ListBox x:Name="LogListBox"
                     ItemsSource="{Binding Logs}" 
                     Background="Transparent" 
                     BorderThickness="0"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="4,2"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Focusable" Value="False"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontFamily="Consolas, Courier New" FontSize="12" Foreground="{DynamicResource MaterialDesignBody}" TextWrapping="Wrap"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Actions -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Click="CloseButton_Click"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Content="CERRAR"
                    IsEnabled="{Binding IsNotRunning}">
            </Button>
        </StackPanel>
    </Grid>
    </Border>
</Window>
