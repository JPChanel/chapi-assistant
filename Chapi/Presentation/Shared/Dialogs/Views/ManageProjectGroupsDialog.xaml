<UserControl x:Class="Chapi.Presentation.Shared.Dialogs.Views.ManageProjectGroupsDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:Chapi.Presentation.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="750"
             MinWidth="720" MaxWidth="820"
             MaxHeight="620">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>

        <Style x:Key="ProjectCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource MaterialDesignCardBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDivider}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource PrimaryHueMidBrush}"/>
                    <Setter Property="Background" Value="{DynamicResource MaterialDesignSelection}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border Background="{DynamicResource MaterialDesignPaper}" 
            BorderBrush="{DynamicResource MaterialDesignDivider}" 
            BorderThickness="1" 
            CornerRadius="8">
        <Grid Margin="20,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 1. Header -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{DynamicResource PrimaryHueMidBrush}" CornerRadius="6" Width="36" Height="36" Margin="0,0,12,0">
                        <materialDesign:PackIcon Kind="FormatListGroup" Width="20" Height="20" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Organizar Proyectos por Grupos" FontSize="18" FontWeight="Bold"/>
                        <TextBlock Text="Arrastra y suelta proyectos entre grupos para organizarlos visualmente" 
                                   FontSize="12" 
                                   Foreground="{DynamicResource MaterialDesignBodyLight}" 
                                   Opacity="0.8" 
                                   Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>

                <Button Grid.Column="1" 
                        Style="{StaticResource MaterialDesignIconButton}" 
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        CommandParameter="false"
                        ToolTip="Cerrar"
                        Width="32" Height="32">
                    <materialDesign:PackIcon Kind="Close" Width="18" Height="18"/>
                </Button>
            </Grid>

            <!-- 2. Action Bar: Create Group + Search -->
            <Border Grid.Row="1" Background="{DynamicResource MaterialDesignCardBackground}" 
                    BorderBrush="{DynamicResource MaterialDesignDivider}" 
                    BorderThickness="1" 
                    CornerRadius="6" 
                    Padding="12,8" 
                    Margin="0,0,0,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="220"/>
                    </Grid.ColumnDefinitions>

                    <!-- Crear Grupo -->
                    <TextBox x:Name="TxtNewGroupName" 
                             Grid.Column="0"
                             materialDesign:HintAssist.Hint="Nombre del nuevo grupo (ej. APIs y Servicios)"
                             VerticalAlignment="Center"
                             KeyDown="TxtNewGroupName_KeyDown"
                             Margin="0,0,10,0"/>

                    <Button Grid.Column="1"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Content="+ CREAR GRUPO"
                            Height="32"
                            Padding="12,0"
                            FontSize="11"
                            FontWeight="SemiBold"
                            Click="BtnCreateGroup_Click"/>

                    <!-- Buscador -->
                    <Grid Grid.Column="3">
                        <TextBox x:Name="TxtSearch"
                                 materialDesign:HintAssist.Hint="Buscar proyecto..."
                                 materialDesign:TextFieldAssist.HasClearButton="True"
                                 VerticalAlignment="Center"
                                 TextChanged="TxtSearch_TextChanged"/>
                    </Grid>
                </Grid>
            </Border>

            <!-- 3. Main Groups Grid / List -->
            <ScrollViewer Grid.Row="2" 
                          VerticalScrollBarVisibility="Auto" 
                          HorizontalScrollBarVisibility="Disabled"
                          MaxHeight="380"
                          Margin="0,0,0,14">
                <ItemsControl x:Name="GroupsItemsControl">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- Group Box / Drop Zone -->
                            <Border Width="350" 
                                    Margin="0,0,12,12" 
                                    Background="{DynamicResource MaterialDesignCardBackground}"
                                    BorderBrush="{Binding BorderBrush}"
                                    BorderThickness="{Binding BorderThickness}"
                                    CornerRadius="8"
                                    AllowDrop="True"
                                    DragEnter="GroupBorder_DragEnter"
                                    DragOver="GroupBorder_DragOver"
                                    DragLeave="GroupBorder_DragLeave"
                                    Drop="GroupBorder_Drop"
                                    Tag="{Binding}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <!-- Group Header -->
                                    <Border Grid.Row="0" 
                                            Background="{DynamicResource MaterialDesignSelection}" 
                                            CornerRadius="8,8,0,0" 
                                            Padding="12,8"
                                            BorderBrush="{DynamicResource MaterialDesignDivider}"
                                            BorderThickness="0,0,0,1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Group Name & Count -->
                                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                <materialDesign:PackIcon Kind="{Binding IconKind}" 
                                                                         Width="16" Height="16" 
                                                                         Foreground="{DynamicResource PrimaryHueMidBrush}" 
                                                                         VerticalAlignment="Center" 
                                                                         Margin="0,0,6,0"/>
                                                <TextBlock Text="{Binding Name}" 
                                                           FontWeight="Bold" 
                                                           FontSize="13" 
                                                           VerticalAlignment="Center"
                                                           MaxWidth="180"
                                                           TextTrimming="CharacterEllipsis"/>
                                                <Border Background="{DynamicResource MaterialDesignDivider}" 
                                                        CornerRadius="10" 
                                                        Padding="6,1" 
                                                        Margin="8,0,0,0"
                                                        VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding VisibleCount}" 
                                                               FontSize="10" 
                                                               FontWeight="SemiBold" 
                                                               Opacity="0.8"/>
                                                </Border>
                                            </StackPanel>

                                            <!-- Group Actions (Rename & Delete) -->
                                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center"
                                                        Visibility="{Binding IsDefault, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                                <Button Style="{StaticResource MaterialDesignIconButton}" 
                                                        Width="26" Height="26" 
                                                        Padding="0"
                                                        ToolTip="Renombrar grupo"
                                                        Click="BtnRenameGroup_Click"
                                                        Tag="{Binding}">
                                                    <materialDesign:PackIcon Kind="PencilOutline" Width="14" Height="14"/>
                                                </Button>
                                                <Button Style="{StaticResource MaterialDesignIconButton}" 
                                                        Width="26" Height="26" 
                                                        Padding="0"
                                                        ToolTip="Eliminar grupo (los proyectos pasarÃ¡n a 'Sin Agrupar')"
                                                        Click="BtnDeleteGroup_Click"
                                                        Tag="{Binding}">
                                                    <materialDesign:PackIcon Kind="TrashCanOutline" Width="14" Height="14" Foreground="#E57373"/>
                                                </Button>
                                            </StackPanel>
                                        </Grid>
                                    </Border>

                                    <!-- Projects Container -->
                                    <Border Grid.Row="1" MinHeight="80" MaxHeight="220" Padding="8">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                                            <StackPanel>
                                                <!-- Empty state -->
                                                <TextBlock Text="Arrastra proyectos aquÃ­" 
                                                           FontSize="11" 
                                                           FontStyle="Italic" 
                                                           Opacity="0.5" 
                                                           HorizontalAlignment="Center" 
                                                           Margin="0,20,0,0"
                                                           Visibility="{Binding HasNoProjects, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                                <!-- Project Cards -->
                                                <ItemsControl ItemsSource="{Binding Projects}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Style="{StaticResource ProjectCardStyle}"
                                                                    Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                    PreviewMouseLeftButtonDown="ProjectCard_PreviewMouseLeftButtonDown"
                                                                    PreviewMouseMove="ProjectCard_PreviewMouseMove"
                                                                    ToolTip="{Binding FullPath}"
                                                                    Tag="{Binding}">
                                                                <Grid>
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                        <ColumnDefinition Width="*"/>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                    </Grid.ColumnDefinitions>

                                                                    <materialDesign:PackIcon Kind="FolderOutline" 
                                                                                             Width="16" Height="16" 
                                                                                             VerticalAlignment="Center" 
                                                                                             Margin="0,0,8,0"
                                                                                             Foreground="{DynamicResource PrimaryHueMidBrush}"/>

                                                                    <TextBlock Grid.Column="1" 
                                                                               Text="{Binding Name}" 
                                                                               FontWeight="SemiBold" 
                                                                               FontSize="12" 
                                                                               VerticalAlignment="Center" 
                                                                               TextTrimming="CharacterEllipsis"/>

                                                                    <materialDesign:PackIcon Grid.Column="2" 
                                                                                             Kind="DragVertical" 
                                                                                             Width="16" Height="16" 
                                                                                             Opacity="0.4" 
                                                                                             VerticalAlignment="Center"/>
                                                                </Grid>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- 4. Footer -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="InformationOutline" Width="16" Height="16" Opacity="0.7" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBlock Text="Los cambios se guardan automÃ¡ticamente. Al eliminar un grupo sus proyectos vuelven a 'Sin Agrupar'." 
                               FontSize="11" 
                               Opacity="0.7" 
                               VerticalAlignment="Center"/>
                </StackPanel>

                <Button Grid.Column="1" 
                        Content="LISTO" 
                        Style="{StaticResource MaterialDesignRaisedButton}" 
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        CommandParameter="true"
                        Height="34"
                        Padding="20,0"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>